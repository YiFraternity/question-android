<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hsx.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" android:label="@string/app_name"></activity>
        <activity android:name=".mainActivity" android:label="@string/app_name" ></activity>
        <activity android:name=".ForgetPwdActivity" android:label="@string/app_name"></activity>
        <activity android:name=".PersonalCenterActivity" android:label="@string/app_name"/>
        <activity android:name=".ResetpwdActivity" android:label="@string/app_name"/>
        <activity android:name=".QuestionlistActivity" android:label="@string/app_name"/>
    </application>

</manifest>